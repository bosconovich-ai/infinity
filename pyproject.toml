[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "idea-factory"
version = "0.1.0"
description = "Manual idea intake service with OpenRouter-backed structuring."
requires-python = ">=3.12"

[project.optional-dependencies]
dev = ["pytest>=8.0"]
